# Routes

The API prefix is `/api/v1`. Authentication endpoints live under `/auth`; protected application endpoints use Sanctum. Administration endpoints are under `/admin`.

See [[API Documentation]].
